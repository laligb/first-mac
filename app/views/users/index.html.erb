<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% User.all.each do |user| %>
    <li>
      <%= render user %>
    </li>
  <% end %>
</ul>


<%= will_paginate %>
